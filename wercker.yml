box: nodesource/trusty:LTS
build:
  steps:
    - script:
        name: npm versions before
        code: |
          npm --versions
    - script:
        name: Update npm so that modules are installed like in my local environment
        code: |
          sudo npm i -g npm
    - script:
        name: npm versions before
        code: |
          npm --versions
    - npm-install
    - npm-test
